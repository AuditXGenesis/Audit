name: notify-on-verify
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'audits/**/issues.final.json'
jobs:
  announce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Install deps
        run: pip install tweepy requests
      - name: Announce placeholder
        run: echo 'Announce script will post to Telegram & X when configured.'
